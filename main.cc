#include <iostream>
#include <cmath>
#include <sstream> 
#include <fstream>
#include "funzioni.h"
using namespace std;


int main(){

    stringstream sout;   //my printer
    int ntot = 0;
    ...*w = load_data(".dat", ntot);

    
    print_and_save(sout);

    delete [] w;

    return 0;
}
